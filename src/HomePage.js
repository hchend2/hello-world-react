import './App.css';

export default function HomePage() {
  return (
    <div className="page home">
      <h2>Welcome to the Home Page</h2>
      <p>This is a simple React application with routing and layout.</p>
    </div>
  );
}

// This code defines a HomePage component for a React application.
// The HomePage component includes a heading and a paragraph describing the application.
// It is styled with a CSS class "page home" to apply specific styles.
// The HomePage component is typically used as a child route in the routing configuration of the application.